#pragma once

#define VelEngine_VERSION_MAJOR "${VelEngine_VERSION_MAJOR}"
#define VelEngine_VERSION_MINOR "${VelEngine_VERSION_MINOR}"

#define SHADERS_PATH "${SHADERS_PATH_SPV}"
#define MESH_PATH "${MESH_PATH}"
#define TEXTURE_PATH "${TEXTURE_PATH}"